import React from "react";
import { Message } from "semantic-ui-react";

const ConfirmEmailMessage = () => (
  <Message info>
    <Message.Header>
      Verify your email to unlock the full application
    </Message.Header>
  </Message>
);

export default ConfirmEmailMessage;
